c = document.getElementById('c');
c.width = 600;
c.height = 400;
ctx = c.getContext('2d');
function xucxac1() {
	ctx.clearRect(0,0,c.width, c.height);
	ctx.fillStyle = 'gray';
	ctx.fillRect(50, 50, 200, 200);
	var so = Math.ceil(Math.random() * 6);
	console.log(so);
	if (so === 1) {
		ctx.fillStyle = 'white';
		ctx.beginPath();
		ctx.arc(150, 150, 20, 0, Math.PI * 2);
		ctx.fill();
	}
	if (so === 2) {
		ctx.fillStyle = 'white';
		ctx.beginPath();
		ctx.arc(100, 100, 20, 0, Math.PI * 2);
		ctx.fill();
		ctx.beginPath();
		ctx.arc(200, 200, 20, 0, Math.PI * 2);
		ctx.fill();
	}
	if (so === 3) {
		ctx.fillStyle = 'white';
		ctx.beginPath();
		ctx.arc(100, 100, 20, 0, Math.PI * 2);
		ctx.fill();
		ctx.beginPath();
		ctx.arc(200, 200, 20, 0, Math.PI * 2);
		ctx.fill();
		ctx.beginPath();
		ctx.arc(150, 150, 20, 0, Math.PI * 2);
		ctx.fill();
	}
	if (so === 4) {
		ctx.fillStyle = 'white';
		ctx.beginPath();
		ctx.arc(100, 100, 20, 0, Math.PI * 2);
		ctx.fill();
		ctx.beginPath();
		ctx.arc(200, 200, 20, 0, Math.PI * 2);
		ctx.fill();
		ctx.beginPath();
		ctx.arc(100, 200, 20, 0, Math.PI * 2);
		ctx.fill();
		ctx.beginPath();
		ctx.arc(200, 100, 20, 0, Math.PI * 2);
		ctx.fill();
	}
	if (so === 5) {
		ctx.fillStyle = 'white';
		ctx.beginPath();
		ctx.arc(100, 100, 20, 0, Math.PI * 2);
		ctx.fill();
		ctx.beginPath();
		ctx.arc(200, 200, 20, 0, Math.PI * 2);
		ctx.fill();
		ctx.beginPath();
		ctx.arc(100, 200, 20, 0, Math.PI * 2);
		ctx.fill();
		ctx.beginPath();
		ctx.arc(200, 100, 20, 0, Math.PI * 2);
		ctx.fill();
		ctx.beginPath();
		ctx.arc(150, 150, 20, 0, Math.PI * 2);
		ctx.fill();
	}
	if (so === 6) {
		ctx.fillStyle = 'white';
		ctx.beginPath();
		ctx.arc(100, 100, 20, 0, Math.PI * 2);
		ctx.fill();
		ctx.beginPath();
		ctx.arc(200, 200, 20, 0, Math.PI * 2);
		ctx.fill();
		ctx.beginPath();
		ctx.arc(100, 200, 20, 0, Math.PI * 2);
		ctx.fill();
		ctx.beginPath();
		ctx.arc(200, 100, 20, 0, Math.PI * 2);
		ctx.fill();
		ctx.arc(100, 150, 20, 0, Math.PI * 2);
		ctx.fill();
		ctx.beginPath();
		ctx.arc(200, 150, 20, 0, Math.PI * 2);
		ctx.fill();
	}
}
function xucxac2() {
	ctx.fillStyle = 'gray';
	ctx.fillRect(50 + 300, 50, 200, 200);
	var so = Math.ceil(Math.random() * 6);
	console.log(so);
	if (so === 1) {
		ctx.fillStyle = 'white';
		ctx.beginPath();
		ctx.arc(150 + 300, 150, 20, 0, Math.PI * 2);
		ctx.fill();
	}
	if (so === 2) {
		ctx.fillStyle = 'white';
		ctx.beginPath();
		ctx.arc(100 + 300, 100, 20, 0, Math.PI * 2);
		ctx.fill();
		ctx.beginPath();
		ctx.arc(200 + 300, 200, 20, 0, Math.PI * 2);
		ctx.fill();
	}
	if (so === 3) {
		ctx.fillStyle = 'white';
		ctx.beginPath();
		ctx.arc(100 + 300, 100, 20, 0, Math.PI * 2);
		ctx.fill();
		ctx.beginPath();
		ctx.arc(200 + 300, 200, 20, 0, Math.PI * 2);
		ctx.fill();
		ctx.beginPath();
		ctx.arc(150 + 300, 150, 20, 0, Math.PI * 2);
		ctx.fill();
	}
	if (so === 4) {
		ctx.fillStyle = 'white';
		ctx.beginPath();
		ctx.arc(100 + 300, 100, 20, 0, Math.PI * 2);
		ctx.fill();
		ctx.beginPath();
		ctx.arc(200 + 300, 200, 20, 0, Math.PI * 2);
		ctx.fill();
		ctx.beginPath();
		ctx.arc(100 + 300, 200, 20, 0, Math.PI * 2);
		ctx.fill();
		ctx.beginPath();
		ctx.arc(200 + 300, 100, 20, 0, Math.PI * 2);
		ctx.fill();
	}
	if (so === 5) {
		ctx.fillStyle = 'white';
		ctx.beginPath();
		ctx.arc(100 + 300, 100, 20, 0, Math.PI * 2);
		ctx.fill();
		ctx.beginPath();
		ctx.arc(200 + 300, 200, 20, 0, Math.PI * 2);
		ctx.fill();
		ctx.beginPath();
		ctx.arc(100 + 300, 200, 20, 0, Math.PI * 2);
		ctx.fill();
		ctx.beginPath();
		ctx.arc(200 + 300, 100, 20, 0, Math.PI * 2);
		ctx.fill();
		ctx.beginPath();
		ctx.arc(150 + 300, 150, 20, 0, Math.PI * 2);
		ctx.fill();
	}
	if (so === 6) {
		ctx.fillStyle = 'white';
		ctx.beginPath();
		ctx.arc(100 + 300, 100, 20, 0, Math.PI * 2);
		ctx.fill();
		ctx.beginPath();
		ctx.arc(200 + 300, 200, 20, 0, Math.PI * 2);
		ctx.fill();
		ctx.beginPath();
		ctx.arc(100 + 300, 200, 20, 0, Math.PI * 2);
		ctx.fill();
		ctx.beginPath();
		ctx.arc(200 + 300, 100, 20, 0, Math.PI * 2);
		ctx.fill();
		ctx.arc(100 + 300, 150, 20, 0, Math.PI * 2);
		ctx.fill();
		ctx.beginPath();
		ctx.arc(200 + 300, 150, 20, 0, Math.PI * 2);
		ctx.fill();
	}
}